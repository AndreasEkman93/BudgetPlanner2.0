<Application x:Class="BudgetPlanner2._0.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:BudgetPlanner2._0.Views"
             xmlns:viewmodels="clr-namespace:BudgetPlanner2._0.ViewModels">
    <Application.Resources>
        <ResourceDictionary>
            <DataTemplate DataType="{x:Type viewmodels:TransactionDetailsViewModel}">
                <local:TransactionDetailsView />
            </DataTemplate>

            <DataTemplate DataType="{x:Type viewmodels:MainViewModel}">
                <local:MainView />
            </DataTemplate>

            <DataTemplate DataType="{x:Type viewmodels:CreateTransactionViewModel}">
                <local:CreateTransactionView />
            </DataTemplate>

            <DataTemplate DataType="{x:Type viewmodels:EditTransactionViewModel}">
                <local:TransactionEditView />
            </DataTemplate>

            <DataTemplate DataType="{x:Type viewmodels:MonthlyForecastViewModel}">
                <local:MonthlyForecastView />
            </DataTemplate>

            <DataTemplate DataType="{x:Type viewmodels:ManageCategoryViewModel}">
                <local:ManageCategoryView />
            </DataTemplate>
            <Style x:Key="GreenRedListBaseOnIsIncome" TargetType="ListViewItem">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsIncome}" Value="False">
                        <Setter Property="Background" Value="#FFFF7F7F"/>
                        <Setter Property="Opacity" Value="100"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding IsIncome}" Value="True">
                        <Setter Property="Background" Value="#FFB2FFB2"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
            <Style x:Key="GreenRedListBaseOnIsTotalAmount" TargetType="ListViewItem">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsNegative}" Value="True">
                        <Setter Property="Background" Value="#FFFF7F7F"/>
                        <Setter Property="Opacity" Value="100"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding IsNegative}" Value="False">
                        <Setter Property="Background" Value="#FFB2FFB2"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="ModernButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="Gray"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Padding" Value="15,8"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                        CornerRadius="8"
                        Padding="{TemplateBinding Padding}"
                        SnapsToDevicePixels="True">
                                <ContentPresenter HorizontalAlignment="Center" 
                                      VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#1976D2"/>
                    </Trigger>

                    <Trigger Property="IsEnabled" Value="False">
                        <Setter Property="Background" Value="#CCCCCC"/>
                        <Setter Property="Foreground" Value="#888888"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
