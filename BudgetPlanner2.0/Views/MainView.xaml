<UserControl x:Class="BudgetPlanner2._0.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BudgetPlanner2._0.Views"
             xmlns:viewmodels="clr-namespace:BudgetPlanner2._0.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel}">
    <Grid ColumnDefinitions="auto,*,120"
      RowDefinitions="Auto,*,Auto,*">
        <TextBlock Text="RECURRING" 
               Grid.Row="0" 
               Grid.Column="0"
               Grid.RowSpan="2"
               VerticalAlignment="Center" 
               HorizontalAlignment="Center"
               FontWeight="Bold" Foreground="Gray" Margin="5">
            <TextBlock.LayoutTransform>
                <RotateTransform Angle="-90" />
            </TextBlock.LayoutTransform>
        </TextBlock>
        <Grid Grid.Row="0" 
          Grid.Column="1"
          Margin="22,10,22,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="140"/>
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="120" />
                <ColumnDefinition Width="100" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="Date" Grid.Column="0" FontWeight="Bold" Foreground="Gray" />
            <TextBlock Text="Description" Grid.Column="1" FontWeight="Bold" Foreground="Gray" />
            <TextBlock Text="Amount" Grid.Column="2" FontWeight="Bold" Foreground="Gray" />
            <TextBlock Text="Category" Grid.Column="3" FontWeight="Bold" Foreground="Gray" />
            <TextBlock Text="Recurrence Type" Grid.Column="4" FontWeight="Bold" Foreground="Gray" />
        </Grid>
        <ListView x:Name="BudgetListView" 
              Grid.Row="1"
              Grid.Column="1"
      ItemsSource="{Binding RecurringTransactions}" 
      SelectedItem="{Binding SelectedRecurringTransaction, Mode=TwoWay}"
      Margin="10">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="100" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="{Binding Date, StringFormat=\{0:yyyy-MM-dd\}}" Grid.Column="0" />
                        <TextBlock Text="{Binding Description}" Grid.Column="1" />
                        <TextBlock Text="{Binding Amount}" Grid.Column="2" />
                        <TextBlock Text="{Binding Category.Name}" Grid.Column="3" />
                        <TextBlock Text="{Binding RecurrenceDisplayName}" Grid.Column="4" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <TextBlock Text="ONE-TIME" 
               Grid.Row="2" 
               Grid.Column="0"
               Grid.RowSpan="2"
               VerticalAlignment="Center" 
               HorizontalAlignment="Center"
               FontWeight="Bold" Foreground="Gray" Margin="5">
            <TextBlock.LayoutTransform>
                <RotateTransform Angle="-90" />
            </TextBlock.LayoutTransform>
        </TextBlock>
        <Grid Grid.Row="2"
          Grid.Column="1"
          Margin="22,10,22,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="140"/>
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="120" />
            </Grid.ColumnDefinitions>

            <TextBlock Text="Date" Grid.Column="0" FontWeight="Bold" Foreground="Gray" />
            <TextBlock Text="Description" Grid.Column="1" FontWeight="Bold" Foreground="Gray" />
            <TextBlock Text="Amount" Grid.Column="2" FontWeight="Bold" Foreground="Gray" />
            <TextBlock Text="Category" Grid.Column="3" FontWeight="Bold" Foreground="Gray" />
        </Grid>
        <ListView x:Name="OneTimeListView" 
              ItemsSource="{Binding OneTimeTransactions}"
              SelectedItem="{Binding SelectedOneTimeTransaction, Mode=TwoWay}"
              Margin="10" 
              Grid.Row="3"
              Grid.Column="1"
              >
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="120" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="{Binding Date, StringFormat=\{0:yyyy-MM-dd\}}" Grid.Column="0" />
                        <TextBlock Text="{Binding Description}" Grid.Column="1" />
                        <TextBlock Text="{Binding Amount}" Grid.Column="2" />
                        <TextBlock Text="{Binding Category.Name}" Grid.Column="3" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Grid Grid.Column="3"
              Grid.RowSpan="4"
              VerticalAlignment="Top"
          >
            <Button Content="Details"
                    Style="{StaticResource ModernButtonStyle}"
                    Command="{Binding GoToDetailsCommand}"
                    Margin="0,10,10,10"
                    Height="40"/>
        </Grid>
    </Grid>
</UserControl>
